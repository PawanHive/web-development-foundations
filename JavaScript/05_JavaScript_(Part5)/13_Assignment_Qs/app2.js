/* Qs Create a object representing a car that stores the following properties for the 
car: name, model, color.
Print the car's name.
*/

const car = {
    name: 'bmw',
    model: '11', 
    color: 'red'
}
console.log(car.name)